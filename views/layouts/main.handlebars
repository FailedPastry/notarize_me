<!DOCTYPE html>
<html lang="en" class="dark">
<head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Notarize Me</title>
    <link rel="stylesheet" type="text/css" href="/css/jass.css">
    <link rel="stylesheet" type="text/css" href="/css/style.css">
    <!-- Add correct path to tailwind.config.js or remove if unnecessary -->
    <link href="../public/css/tailwind.config.js" rel="stylesheet">
    <!-- Add correct paths or CDN links for React scripts -->
    <script src="path/to/react.js"></script>
    <script src="path/to/react-dom.js"></script>
    <link href="https://cdn.jsdelivr.net/npm/tailwindcss@2.2.19/dist/tailwind.min.css" rel="stylesheet">
    <style>
        .dark-mode {
            background-color: #141F6D;
            color: #668CC2;
        }
    </style>
</head>
<body class="bg-white text-#B6B9CE dark-mode">
    <button id="toggleDarkMode">X</button>

    <script>
        const toggleDarkModeBtn = document.getElementById('toggleDarkMode');
        const body = document.body;

        toggleDarkModeBtn.addEventListener('click', () => {
            body.classList.toggle('dark-mode');
        });
    </script>
    <!-- start of main nav bar -->
    <nav class="navbar navbar-expand-lg navbar-dark bg-danger">
        <div class="container d-block">
            <!-- Navbar content -->
        </div>
    </nav>
    <!-- end of main navigation bar -->
    <nav>
        <button class="btn" id="logout">logout</button>
        <a class="navbar-brand ml-2" href="https://">Notarize Me</a>
    </nav>
    
    <!-- Include the login card partial -->
    {{#if isAuthenticated}}
    <div class="card">
        <div class="card-body">
            <h5 class="card-title">Main Section</h5>
            <div id="main-section">
                <!-- Main section content goes here -->
            </div>
            <p class="card-text">Welcome to the main section</p>
        </div>
    </div>
    {{else}}
    <div class="card">
        <div class="card-body">
            <button class="btn" id="login">
            <h5 class="card-title">Login</h5>
            </button>
            <div id="login-card">Please log in to access the main section</div>
            <p class="card-text">Login to your account</p>
        </div>
    </div>
    {{/if}}
    
    <!-- aside hidden nav buttons -->
    <!-- Include other content here -->
    
    <!-- end of main navigation bar -->
    {{!-- aside hidden nav buttons --}}
    
    <main role="main" class="container mt-3 mb-3">
        <div class="row">
            <div class="col-lg-3">
                <ul class="nav nav-left flex-column card p-1 d-none d-lg-flex shadow-sm border-0">
                    {{#each menuItems}}
                    <li class="nav-item {{menuClass}}">
                        <a class="nav-link h4" href="{{url}}">
                            <svg id="{{svgId}}" viewBox="0 0 32 32" width="32" height="32" fill="none" stroke="currentcolor" stroke-linecap="round" stroke-linejoin="round" stroke-width="2" class="text-danger svg-icon">
                                {{svgPath}}
                            </svg> {{menuItemText}}
                        </a>
                    </li>
                    {{/each}}
                </ul>
            </div>
            {{editdraft}}
            <!-- main display page -->
           
                            <div class="card-body pt-0">
                                <ul class="list-group list-group-flush">
                                    <div class="row mt-3 ml-3">
                                        <div class


                                        </div>

    </main>
</body>

</html>
